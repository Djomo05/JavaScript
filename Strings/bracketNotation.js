/**
 * In Javascript a string a sequence of character.
 *
 * To access a character, you simply need  to use the bracketNotation
 *
 * **/

// To access the first character from the hello
let greeting = "Hello";
console.log(greeting[0]);

// To access the last character from the hello
console.log(greeting[greeting.length - 1]);

// We can also contactaned characters access from a string
console.log(greeting[0] + " " + greeting[1]);